<script context="module">export const metadata = {"text":"<p>So in SvelteKit, we don&#39;t just colocate in files, we colocate in directories. In a SvelteKit app, each directory is a route, and each route can contain server code for loading data, and view code that runs on both the server and the client. In this screenshot we&#39;re looking at a SvelteKit codebase — this is actually the demo app you get when you do <code>npm create svelte</code> — and inside <code>src/routes</code> we&#39;ve got an <code>/about</code> route and a <code>/sverdle</code> route, which is a Wordle clone that works without JavaScript.</p>\n<p>The page&#39;s server code goes in a <code>+page.server.js</code> file, and any file with that suffix cannot be imported into client-side code — your app will simply fail to build.</p>\n<p>Directory-based routing is something we stole from Next.js, and it has a list of other benefits that&#39;s too long to get into right now, but I strongly believe that this is the way. One little Easter egg I&#39;ll show you is that on the right, in <code>+page.svelte</code>, on line 16 we&#39;re hovering over <code>data.guesses</code> and it&#39;s giving us the type and the inline documentation that we wrote on the left on line 9. So we get type safety across the network. This is the sort of thing that&#39;s made possible by embracing strong conventions.</p>\n","title":"Directory-based routing","classnames":"","styles":[]};</script>

<img alt="VSCode showing a SvelteKit project" src="./images/vscode.png" style="width: 90em" />
