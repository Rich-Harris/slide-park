<script context="module">export const metadata = {"text":"<p>Here&#39;s another example of a build step being really beneficial. All of the code snippets in SvelteKit&#39;s documentation are typechecked against the SvelteKit source code, and that unavoidably takes time — one of these pages might take several seconds to render on this M1 Macbook Pro. So we prerender this content at build time — as well as ensuring that we don&#39;t publish incorrect documentation, because if we do, the deployment will fail, it means that everyone visiting this site gets content instantly.</p>\n<p>If we did that rendering on demand instead, then even if we had incredibly sophisticated caching, some users would end up having to wait multiple seconds for the page to load, and that&#39;s just not acceptable.</p>\n<p>So: build steps — you might not need one, but you should probably have one anyway.</p>\n","title":"Typechecked docs","classnames":"","styles":[]};</script>

<h1 style="color: hotpink">Typechecked docs</h1>