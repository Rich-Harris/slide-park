<script context="module">export const metadata = {"text":"<p>But the thing that often gets missed in discussions about build steps is that they primarily exist for our users&#39; benefit, not ours.</p>\n<p>Build steps let us minify code, combine 100 small modules into 10 coarse-grained chunks that load faster, they remove unused code from client-side bundles, they optimise images, they hash assets so that they can be cached immutably, the guard against bugs by typechecking and linting. This is all stuff that directly results in a better user experience.</p>\n<p>It&#39;s true that build steps also benefit developers by letting us use non-standard dialects and easily import dependencies, and so on. But if you remove build steps, it&#39;s user experience that suffers more than developer experience.</p>\n","title":"UX over DX","classnames":"","styles":[]};</script>

<h1>build tools are all about user experience</h1>
<ul>
	<li>✅ minifying code</li>
	<li>✅ bundling code</li>
	<li>✅ removing unused code</li>
	<li>✅ optimising images</li>
	<li>✅ hashing assets</li>
	<li>✅ preventing bugs</li>
</ul>
