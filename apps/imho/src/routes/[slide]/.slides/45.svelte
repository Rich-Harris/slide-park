<script context="module">export const metadata = {"text":"<p>All this is why I&#39;m personally a little bit nervous about React Server Components. For those of you who aren&#39;t aware, RSC means that your UI is rendered on the server by default, and only components that have a special &#39;use client&#39; annotation will run in the browser, and have access to things like <code>useState</code> and <code>useEffect</code>. Server components and client components have different APIs, they can use different hooks, and there are restrictions on how you can combine them, but the upshot is that by default your React app will render entirely on the server, and you&#39;ll opt in to client rendering for specific parts of your app.</p>\n<p>Now clearly this has some very substantial benefits — this is hugely important work and it frankly blows my mind. But I do worry that we&#39;ve become so paranoid about serving JavaScript to our users that we&#39;re overcorrecting. If the default is for interaction to require a server round-trip, and for client components to be implicitly regarded as something to avoid, then the net result could be slower and less resilient apps. I might just be fear-mongering here, I don&#39;t know. Dan Abramov will probably do a withering tweet thread on why I&#39;m wrong. I&#39;m very curious to see how all this looks in a couple of years with the benefit of hindsight.</p>\n","title":"React Server Components","classnames":"","styles":[]};</script>

<h1>React Server Components</h1>