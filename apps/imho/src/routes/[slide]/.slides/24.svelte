<script context="module">export const metadata = {"text":"<p>HTML, on the other hand, is a really good language for describing UI. There&#39;s no temporal aspect to it, it&#39;s almost a physical substance, like clay, or at least that&#39;s how I think of it. The catch, of course, is that HTML is static, so you can&#39;t use it to describe things with rich interactivity. But what if we use HTML as our starting point and create a new DSL?</p>\n","title":"HTML is clay","classnames":"","styles":[]};</script>

<pre><code>&lt;h1&gt;Hello world&lt;/h1&gt;</code></pre>
